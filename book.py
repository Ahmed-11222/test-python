class Library:
    def __init__(self)
    self.book=[]
    
    def add_book(list1,title, author, copies)
        list1=[]
        list1.append(title)
        list1.append(author)
        list1.append(copies)
        self.book.append(list_)
    def display_books(self):
        for i in self.library
        print(self.book)
        #print("Title {} Author {} Copies{}".format(self,self.title, self.author,self.copies))
        
    
